
spring:
  application:
    name: provider-ck

  cloud:

    config:

      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: springcloud-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka